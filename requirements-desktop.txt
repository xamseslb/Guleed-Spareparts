# Extra dependencies for the offline desktop app (on top of backend/requirements.txt)
pywebview>=5.0      # shows the app in its own native window
pyinstaller>=6.0    # builds the standalone .exe (build machine only)
